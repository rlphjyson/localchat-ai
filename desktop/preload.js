// Nothing exposed to the page yet -- the app is currently just the existing web UI shown in a
// native window. Add contextBridge APIs here if/when the desktop wrapper needs to offer the page
// something a browser tab genuinely can't (e.g. native file dialogs).
